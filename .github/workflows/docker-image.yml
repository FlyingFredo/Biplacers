name: Docker Image CI

on:
  push:
    branches: [ "jules_wip_4767422011928057467" ]
  pull_request:
    branches: [ "jules_wip_4767422011928057467" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
